{
  "name": "targettrail",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-backed drug repurposing research workspace.",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "check": "node --check app.js && node --check backend/src/handler.mjs",
    "test": "node --test backend/test/scoring.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
